import { render } from "@testing-library/react";
import Profile from "../../components/Profile";

describe("Given Profile component", () => {
  it("should rener Profile component", () => {
    render(<Profile />);
  });
});
